{% extends "layout.html.twig" %}
    {% block title %}SunDungeon - {{ parent() }}{% endblock %}
    {% block content %}
        <div class="col-md-12">
            <h1>SunDungeon</h1>
        </div>
        <div class="col-md-12">
            <h2>Hellfire</h2>
            {% for zone in hellfire %}
                <div class="col-md-4">
                    <h3><a href="{{ app.request.basepath }}/dungeon/{{ zone.id }}">{{ zone.name }}</a> - {{ (zone.tested / zone.total * 100)[:5] }}%</h3>
                    <p class="col-md-12">
                        <strong>Total creatures:</strong> {{ zone.total }}<br />
                        <strong>Total tested:</strong> {{ zone.tested }}
                    </p>
                    <p class="col-md-4">
                        <strong>Success:</strong> {{ zone.success + zone.no }}
                    </p>
                    <p class="col-md-4">
                        <strong>Bugged:</strong> {{ zone.bugged }}
                    </p>
                    <p class="col-md-4">
                        <strong>Working:</strong> {{ zone.working }}
                    </p>
                    <div class="progress col-md-12" style="padding: 0;">
                        <div class="progress-bar progress-bar-success" style="width: {{ ((zone.success + zone.no) /  (zone.total * 14)) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-warning" style="width: {{ zone.working / (zone.total * 14) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-danger" style="width: {{ zone.bugged / (zone.total * 14) * 100 }}%">
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        <div class="col-md-12">
            <h2>Coilfang</h2>
            {% for zone in coilfang %}
                <div class="col-md-4">
                    <h3><a href="{{ app.request.basepath }}/dungeon/{{ zone.id }}">{{ zone.name }}</a> - {{ (zone.tested / zone.total * 100)[:5] }}%</h3>
                    <p class="col-md-12">
                        <strong>Total creatures:</strong> {{ zone.total }}<br />
                        <strong>Total tested:</strong> {{ zone.tested }}
                    </p>
                    <p class="col-md-4">
                        <strong>Success:</strong> {{ zone.success + zone.no }}
                    </p>
                    <p class="col-md-4">
                        <strong>Bugged:</strong> {{ zone.bugged }}
                    </p>
                    <p class="col-md-4">
                        <strong>Working:</strong> {{ zone.working }}
                    </p>
                    <div class="progress col-md-12" style="padding: 0;">
                        <div class="progress-bar progress-bar-success" style="width: {{ ((zone.success + zone.no) /  (zone.total * 14)) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-warning" style="width: {{ zone.working / (zone.total * 14) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-danger" style="width: {{ zone.bugged / (zone.total * 14) * 100 }}%">
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        <div class="col-md-12">
            <h2>Auchindoun</h2>
            {% for zone in auchindoun %}
                <div class="col-md-4">
                    <h3><a href="{{ app.request.basepath }}/dungeon/{{ zone.id }}">{{ zone.name }}</a> - {{ (zone.tested / zone.total * 100)[:5] }}%</h3>
                    <p class="col-md-12">
                        <strong>Total creatures:</strong> {{ zone.total }}<br />
                        <strong>Total tested:</strong> {{ zone.tested }}
                    </p>
                    <p class="col-md-4">
                        <strong>Success:</strong> {{ zone.success + zone.no }}
                    </p>
                    <p class="col-md-4">
                        <strong>Bugged:</strong> {{ zone.bugged }}
                    </p>
                    <p class="col-md-4">
                        <strong>Working:</strong> {{ zone.working }}
                    </p>
                    <div class="progress col-md-12" style="padding: 0;">
                        <div class="progress-bar progress-bar-success" style="width: {{ ((zone.success + zone.no) /  (zone.total * 14)) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-warning" style="width: {{ zone.working / (zone.total * 14) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-danger" style="width: {{ zone.bugged / (zone.total * 14) * 100 }}%">
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        <div class="col-md-12">
            <h2>Cavern of Time</h2>
            {% for zone in time %}
                <div class="col-md-4">
                    <h3><a href="{{ app.request.basepath }}/dungeon/{{ zone.id }}">{{ zone.name }}</a> - {{ (zone.tested / zone.total * 100)[:5] }}%</h3>
                    <p class="col-md-12">
                        <strong>Total creatures:</strong> {{ zone.total }}<br />
                        <strong>Total tested:</strong> {{ zone.tested }}
                    </p>
                    <p class="col-md-4">
                        <strong>Success:</strong> {{ zone.success + zone.no }}
                    </p>
                    <p class="col-md-4">
                        <strong>Bugged:</strong> {{ zone.bugged }}
                    </p>
                    <p class="col-md-4">
                        <strong>Working:</strong> {{ zone.working }}
                    </p>
                    <div class="progress col-md-12" style="padding: 0;">
                        <div class="progress-bar progress-bar-success" style="width: {{ ((zone.success + zone.no) /  (zone.total * 14)) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-warning" style="width: {{ zone.working / (zone.total * 14) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-danger" style="width: {{ zone.bugged / (zone.total * 14) * 100 }}%">
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
        <div class="col-md-12">
            <h2>Tempest Keep</h2>
            {% for zone in tk %}
                <div class="col-md-4">
                    <h3><a href="{{ app.request.basepath }}/dungeon/{{ zone.id }}">{{ zone.name }}</a> - {{ (zone.tested / zone.total * 100)[:5] }}%</h3>
                    <p class="col-md-12">
                        <strong>Total creatures:</strong> {{ zone.total }}<br />
                        <strong>Total tested:</strong> {{ zone.tested }}
                    </p>
                    <p class="col-md-4">
                        <strong>Success:</strong> {{ zone.success + zone.no }}
                    </p>
                    <p class="col-md-4">
                        <strong>Bugged:</strong> {{ zone.bugged }}
                    </p>
                    <p class="col-md-4">
                        <strong>Working:</strong> {{ zone.working }}
                    </p>
                    <div class="progress col-md-12" style="padding: 0;">
                        <div class="progress-bar progress-bar-success" style="width: {{ ((zone.success + zone.no) /  (zone.total * 14)) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-warning" style="width: {{ zone.working / (zone.total * 14) * 100 }}%">
                        </div>
                        <div class="progress-bar progress-bar-danger" style="width: {{ zone.bugged / (zone.total * 14) * 100 }}%">
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    {% endblock %}
    {% block script %}
    {% endblock %}